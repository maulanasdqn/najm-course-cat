import { FC, ReactElement } from "react";

export const Component: FC = (): ReactElement => {
  return <section className="flex flex col justify-center items-center w-full">Hallo</section>;
};

export default Component;
